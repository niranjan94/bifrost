language: go

go:
  - "1.x"

before_install:
  - go mod download

script:
  - go build -v -ldflags="-s -w"

deploy:
  - provider: script
    skip_cleanup: true
    script: curl -sL https://git.io/goreleaser | bash
    on:
      tags: true