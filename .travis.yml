language: go

go:
  - "1.x"

before_install:
  - make deps

script:
  - make build

deploy:
  - provider: script
    skip_cleanup: true
    script: curl -sL https://git.io/goreleaser | bash
    on:
      tags: true